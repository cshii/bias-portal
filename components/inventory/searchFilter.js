// TODO: re-archetect into proper files



const keywordFilter = ({keywords}) => {
    // keywords comes in as an array of words
    
    let filteredInventory;


    return filteredInventory;
};

module.exports= {keywordFilter}